const routesConfig = {
  root: '/',
  following: '/following',
  user: '/user/:nickname',
  search: '/search',
  upload: '/upload',
  notfound: '*',
  live: '/live',
};


export default routesConfig;
